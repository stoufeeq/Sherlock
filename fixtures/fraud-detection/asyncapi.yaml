asyncapi: 2.6.0
info:
  title: Fraud Detection (event consumer)
  version: 1.0.0
servers:
  redpanda:
    url: localhost:9092
    protocol: kafka
channels:
  banking.transactions.created:
    subscribe:
      operationId: onTransactionCreated
      message:
        name: TransactionCreatedEvent
        contentType: application/json
