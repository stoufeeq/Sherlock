# web-portal-bff

Backend-for-frontend for the web banking portal.

## Dependencies

- `account-service` (REST)
- `transaction-service` (REST)
- `customer-service` (REST)

## Run

```bash
npm install
npm start
```
