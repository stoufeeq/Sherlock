# shared-domain-lib

Shared DTOs, enums, and event schemas for the banking Java services.

Consumed by: `account-service`, `transaction-service`, `customer-service`, `notification-service`.

## Build

```bash
mvn clean install
```
